/**
 * rest operator  is used in array or objects in case of destructuring we,can use it will return rest elements as an array
 * we can use it in rest parameter for n number of arguments it will return array of arguments
 * rest operator should place in the last part of parameter
 * to separate rest and spread operator rest will be used before assingment and it will be used in parameter .spread will be used in the argument
 * 
 * 
 */