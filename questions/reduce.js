const arr = [
    'red', 'blue', 'green', 'yellow', 'orange',
    'purple', 'pink', 'brown', 'cyan', 'magenta',
    'indigo', 'violet', 'turquoise', 'lavender', 'teal',
    'olive', 'maroon', 'navy', 'silver', 'gold',
    'red', 'blue', 'green', 'yellow', 'orange',
    'purple', 'pink', 'brown', 'cyan', 'magenta'
  ];
  
  // find count of green using reduce
  // return the longest string using reduce
  // find the count of characters using reduce
  //const count= arr.reduce((acc,val)=>val=="green"?acc+1:acc,0)
  
  //const long= arr.reduce((acc,val)=>{return val.length>acc.length?val:acc})
  
  //const total=arr.reduce((acc,val)=>acc+val.length,0)
  